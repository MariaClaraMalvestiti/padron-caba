sap.ui.define([
  "sap/ui/core/UIComponent"
], function (UIComponent) {
  "use strict";

  return UIComponent.extend("padrones.taxupload.Component", {
    metadata: {
      manifest: "json"
    }
  });
});
